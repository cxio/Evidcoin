# DECISION-0008: 铸凭哈希碰撞处理

## Status（状态）

Accepted

## Context（背景）

`docs/conception/1.共识-历史证明（PoH）.md` 已明确择优池按铸凭哈希字节序从小到大排序，值小者优。它没有定义两个不同铸凭交易产生相同铸凭哈希时的入池规则。

## Decision（决策）

若两个不同铸凭交易产生完全相同的铸凭哈希，节点只保留先到达择优池的候选者，拒绝后到达的同哈希候选者。

此规则只处理不同 `mintTxID` 的哈希碰撞，不处理同一铸造者在分叉上的双签场景。

## Rationale（理由）

- 铸凭哈希为 64 字节，真实碰撞概率极低。
- 先到先得与择优池的实时传播模型一致。
- 引入额外排序字段会增加协议复杂度，但几乎不会带来实际收益。

## Consequences（影响）

- 择优池实现需要以铸凭哈希为唯一键之一检测重复。
- 极端碰撞导致不同节点先到顺序不一致时，后续分叉选择机制处理结果。

## Conception Relationship（与构想关系）

- 补充择优池同值碰撞的边界语义。
- 不改变 conception 已明确的“值小者优”规则。

## Source（来源）

- 迁移重写自旧 `ADR-0011`。
